// D1 access via getCloudflareContext (OpenNext/Workers runtime).
// STUB — no implementation until Build Plan Phase 1 (gated on Phase 0a:
// D1 write limits vs pageview volume — a sampling decision changes the writes).
// Tables: touches, pageviews, dead_letters — see migrations/001_init.sql.
